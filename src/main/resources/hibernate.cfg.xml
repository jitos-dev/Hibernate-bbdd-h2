<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.h2.Driver</property>
        <!--
            Tipos de conexion a h2:
            la primera (jdbc:h2:mem:/...) es una bbdd en memoria que cuando acaba la ejecucion del programa
            se destruye
            La segunda (jdbc:h2:file/...) es una bbdd con su archivo que aunque cerremos el programa guarda los datos
            Para el ejemplo creo una carpeta que llamo bbdd_h2 (por defecto se crea en C:Users/Juanjo) donde se
            guarda el archivo que va a contener la bbdd
         -->
        <property name="connection.url">jdbc:h2:mem:/prueba_hibernate</property>
  <!--      <property name="connection.url">jdbc:h2:file:~/bbdd_h2/prueba_hibernate'</property>-->

        <property name="connection.username">root</property>
        <property name="connection.password"/>
        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">1</property>
        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.H2Dialect</property>
        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>
        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>
        <!-- Disable the second-level cache -->
<!--        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>-->
        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>
<!--        <property name="hbm2ddl.auto">create-drop</property>-->

        <!-- dbcp connection pool configuration -->
<!--        <property name="hibernate.dbcp.initialSize">5</property>
        <property name="hibernate.dbcp.maxTotal">20</property>
        <property name="hibernate.dbcp.maxIdle">10</property>
        <property name="hibernate.dbcp.minIdle">5</property>
        <property name="hibernate.dbcp.maxWaitMillis">-1</property>-->

        <!--Aqui van las entidades que queremos guardar en bbdd-->
        <mapping class="com.garciajuanjo.hibernatebbddh2.entity.User"/>

    </session-factory>

</hibernate-configuration>